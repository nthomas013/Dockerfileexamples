FROM alpine/ansible:2.18.6
RUN apk update && apk add --no-cache \
    openssl \
    ca-certificates \
    python3 \
    py3-pip \
    libffi-dev \
    openssl-dev \
    build-base \
    git \
    bash \
    sshpass
WORKDIR /ansible
VOLUME /ansible/playbooks
CMD ["ansible"]
